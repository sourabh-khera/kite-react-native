import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  inputDefaultStyles: {
    paddingVertical: 10,
    borderRadius: 5,
    width: '85%',
    paddingLeft: 12,
    marginTop: 20,
    fontSize: 15,
    borderColor: '#e2e2e2',
    borderWidth: 1,
  },
});

export default styles;
